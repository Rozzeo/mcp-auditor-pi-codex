# Copy to .env and fill in ONE of these (never commit .env — it's gitignored).
# Used by `mcp-audit intel autodraft --use-llm` / `mcp-audit intel distill --use-llm`.

GEMINI_API_KEY=
# ANTHROPIC_API_KEY=
