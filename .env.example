# Copy to .env and fill this in (never commit .env — it's gitignored).
# Only needed for the OFF-BY-DEFAULT LLM paths:
#   mcp-audit intel distill --use-llm
#   mcp-audit intel autodraft --use-llm
# Everything else — audits, matrices, `intel fetch`, `intel curate` — runs with
# no key and makes no model calls.

ANTHROPIC_API_KEY=
